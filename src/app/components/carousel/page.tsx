import Image from "next/image";
import styles from "./carousel.module.scss";

export default function Carousel() {
    return (
        <section className={styles.carouselMainContainer}>
      
        </section>
    );
}
